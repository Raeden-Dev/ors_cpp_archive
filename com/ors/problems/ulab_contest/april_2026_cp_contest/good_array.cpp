#include <bits/stdc++.h>
using namespace std;

#define all(a) a.begin(),a.end()
#define FastRead ios_base::sync_with_stdio(0);cin.tie(0)

int main() {
    int n, sum = 0;
    cin >> n;
    vector<int> arr;
    map<int, int> freq;
    for (int i = 0; i < n; i++) {
        int k;
        cin >> k;
        arr[i] = k;
        sum = sum + k;
    }
    for (int j = 0; j < n; j++) {

    }
}